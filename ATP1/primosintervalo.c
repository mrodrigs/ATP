#include <stdio.h>
#include <stdlib.h>

int main(void){
int a, b,

    printf("Inicio do intervalo:   ");
    scanf("%d", &a);
    printf("Fim do intervalo:   ");
    scanf("%d", &b);

    if (a < b){
        while (a <= b){
            for
        }
    }
    else {
        printf("ERRO!");
    }
}
